export const TileSize = 48; //tamanho do quadrado do tabuleiro

export const HeadOffset = 12; //cabeça dos personagens que ultrapassam pela animação

export const DemonTileSize = TileSize; 

export const GameSize = 20 * TileSize; //960px

export enum EnumDirection {
  Left = "ArrowLeft",
  Right = "ArrowRight",
  Up = "ArrowUp",
  Down = "ArrowDown"
}